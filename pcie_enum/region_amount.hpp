#include <iostream>
#include <fstream>
#include <sstream>
#include <string>
#include <filesystem>

std::string aaa(std::string &content, int number);
int getPciDeviceRegionAmount(std::string& deviceAddress);